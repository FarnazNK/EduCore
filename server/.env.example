# ============================================
# EduCore Server Environment Variables
# ============================================
# 
# ⚠️ SECURITY WARNING:
# This is an EXAMPLE file only!
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace ALL placeholder values with real credentials
# 3. NEVER commit the .env file to Git!
# 
# ============================================

# Application
NODE_ENV=development
PORT=3000

# ============================================
# Database Configuration
# ============================================
# SECURITY: Use a strong, unique password in production!
# Example format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://educore:CHANGE_THIS_PASSWORD@localhost:5432/educore_db

# ============================================
# Redis Configuration
# ============================================
REDIS_URL=redis://localhost:6379

# ============================================
# JWT Configuration
# ============================================
# CRITICAL: Generate strong random secrets for production!
# Never use these example values in production!
# 
# Generate secure secrets with:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 
JWT_SECRET=REPLACE_WITH_STRONG_RANDOM_64_CHAR_STRING
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# ============================================
# CORS Configuration
# ============================================
# SECURITY: Set to your actual frontend domain in production
# NEVER use '*' in production!
CORS_ORIGIN=http://localhost:5173
# Production example: https://yourdomain.com

# ============================================
# Email Configuration (Optional)
# ============================================
# For Gmail, use App Passwords: https://myaccount.google.com/apppasswords
# For other providers, check their SMTP settings
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@example.com
EMAIL_PASSWORD=YOUR_APP_SPECIFIC_PASSWORD_HERE
EMAIL_FROM=noreply@educore.com

# ============================================
# File Storage (MinIO/S3)
# ============================================
# SECURITY: Change these default credentials in production!
STORAGE_PROVIDER=minio
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=CHANGE_THIS_ACCESS_KEY
MINIO_SECRET_KEY=CHANGE_THIS_SECRET_KEY
STORAGE_BUCKET=educore

# For AWS S3, use:
# STORAGE_PROVIDER=s3
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# S3_BUCKET=your-bucket-name

# ============================================
# Stripe Payment Configuration (Optional)
# ============================================
# Get your keys from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_REPLACE_WITH_YOUR_STRIPE_SECRET_KEY
STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_WITH_YOUR_STRIPE_PUBLISHABLE_KEY
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_WITH_YOUR_WEBHOOK_SECRET

# ============================================
# Rate Limiting
# ============================================
# Window in milliseconds (default: 15 minutes)
RATE_LIMIT_WINDOW_MS=900000
# Max requests per window
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# Production Deployment Notes
# ============================================
# 
# For production, ensure you:
# 1. Use strong, unique passwords (32+ characters)
# 2. Enable HTTPS only
# 3. Set restrictive CORS origins
# 4. Use environment-specific secrets
# 5. Enable monitoring and logging
# 6. Set up automated backups
# 7. Use a secrets management service (AWS Secrets Manager, HashiCorp Vault)
# 
# ============================================
